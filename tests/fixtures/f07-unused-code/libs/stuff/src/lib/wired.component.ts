import { Component } from '@angular/core';

@Component({
  selector: 'fix-wired',
  template: '<span>wired but never rendered</span>',
  standalone: true,
})
export class WiredNotRenderedComponent {}
