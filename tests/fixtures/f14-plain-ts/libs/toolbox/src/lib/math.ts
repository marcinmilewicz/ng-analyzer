export function add(a: number, b: number): number {
  return a + b;
}

export function unusedMultiply(a: number, b: number): number {
  return a * b;
}

export const PI_ISH = 3.14;

export enum RoundingMode {
  Up,
  Down,
}
